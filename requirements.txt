# Core dependencies
torch>=2.0.0
transformers>=4.40.0
tilelang>=0.1.0

# FlashQLA (DeltaNet linear attention) - required for DeltaNet prefill
# Install manually from source:
#   git clone https://github.com/QwenLM/FlashQLA.git
#   cd FlashQLA && pip install -v .
flash-qla @ git+https://github.com/QwenLM/FlashQLA.git

# Optional: for kernel benchmark comparison only (not required for inference)
# flash-attn==2.7.1.post4

pytest
